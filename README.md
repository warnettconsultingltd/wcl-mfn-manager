# wcl-mfn-manager-rest-api

This project contains Spring Boot server applications providing REST API endpoints.

## Testing

Unit tests and Integration tests are provided.

Unit tests are to exist within src/test/java ad are run via mvn test

Integration tests exist within src/integration-tests/java and are run via mvn integration-test.  These tests are also run when mvn verify is invoked.